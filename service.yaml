apiVersion: v1
kind: Service
metadata:
  name: jenkins-demo-deployment-test-service
spec:
  selector:
    app: jenkins-demo-deployment-test
  ports:
  - protocol: TCP
    port: 80
    targetPort: 1323
  type: LoadBalancer
